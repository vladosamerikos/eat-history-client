name: eat-history-web-dev

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: eh-web
    env_file: .env
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
